# it is recommended to configure local Git client to use "main" as the name for the default branch
# this is the same default branch name if our first commit was performed using CodeCommimt console
git config --global init.defaultBranch main

# optional: to suppress Git safecrlf warnings on Windows
git config --global core.safecrlf false
